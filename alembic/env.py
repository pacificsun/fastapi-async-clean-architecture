# Placeholder Alembic env.py for async setup
# Replace with alembic-generated env.py when running `alembic init`.

from logging.config import fileConfig

# this is the Alembic Config object, which provides
# access to the values within the .ini file in use.


def run_migrations():
    raise RuntimeError("Run `alembic init` and configure env.py for async migrations.")


if __name__ == '__main__':
    run_migrations()
